# Note: Chrome extensions do not read .env files natively without a build process like Webpack or Vite.
# This file serves as documentation for what should be configured in src/utils/constants.js

# The URL of the backend API
API_BASE_URL="http://localhost:8000"

# Maximum length of text to extract from a webpage
MAX_TEXT_LENGTH=5000
